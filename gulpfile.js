import {
  clean,
  cleanAll,
  build,
  buildFast,
  dev,
  preview,
  devCheck,
  previewCheck,
} from '#gulp/public/index.js'

export { clean, cleanAll, build, buildFast, preview, devCheck, previewCheck }
export default dev

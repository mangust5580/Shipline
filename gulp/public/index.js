import {
  clean,
  cleanAll,
  build,
  buildFast,
  dev,
  preview,
  devCheck,
  previewCheck,
} from '#gulp/index.js'

export { clean, cleanAll, build, buildFast, dev, preview, devCheck, previewCheck }
